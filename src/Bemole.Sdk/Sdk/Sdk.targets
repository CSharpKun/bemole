<Project>

    <PropertyGroup Condition="'$(MSBuildProjectExtension)' == '.bmproj'">
        <LanguageTargets>$([MSBuild]::NormalizePath('$(MSBuildThisFileDirectory)', '..\Core\Bemole.Core.targets'))</LanguageTargets>
    </PropertyGroup>

    <Import Sdk="Microsoft.NET.Sdk" Project="Sdk.targets"/>

</Project>